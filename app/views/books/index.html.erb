
  <p id ="notice">
    <%= notice %>
  </p>

<h1>
  <strong>
    Books
  </strong>
</h1>

<table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></tb>
        <td><%= book.body %></tb>
        <td><%= link_to "Show",book_path(book.id) %></tb>
        <td><%= link_to "Edit" ,edit_book_path(book.id) %></tb>
        <td><%= link_to "Destroy", destroy_book_path(book.id), method: :delete,"data-confirm" => "Are you sure?"%></tb>
      </tr>
    </tbody>
    <% end %>
</table>



<h2>
  <strong>New book</strong>
</h2>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<div class="form">
<%= form_with model: @book, url: '/books', method: :post, local: true do |f| %>
    <div class="field">
      <h4>Title</h4>
      <%= f.text_field :title %>
    </div>

    <div class="field">
      <h4>Body</h4>
      <%= f.text_area :body %>
    </div>

  <div class="action"><%= f.submit"Create Book" %></div>
</div>
<% end %>